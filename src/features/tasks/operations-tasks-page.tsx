"use client";

import { useActionState } from "react";
import { CheckCircle2, CircleAlert, ClipboardList, Clock3, LoaderCircle, Plus, Wrench } from "lucide-react";
import { useCommandForm } from "@/features/shared/use-command-form";

export type OperationsTaskItem = Readonly<{
  id: string;
  taskType: string;
  title: string;
  description: string | null;
  status: "open" | "in_progress" | "completed" | "cancelled";
  dueAt: string | null;
  bookingId: string | null;
  assignedMembershipId: string | null;
  createdAt: string;
  updatedAt: string;
}>;

export type TaskActionState = Readonly<{
  status: "idle" | "success" | "invalid" | "denied" | "retry";
  message: string;
}>;

export type CreateTaskAction = (previousState: TaskActionState, formData: FormData) => Promise<TaskActionState>;
export type UpdateTaskStatusAction = (taskId: string, status: string, formData?: FormData) => Promise<void>;

const initialState: TaskActionState = { status: "idle", message: "" };
const taskTypeLabel: Record<string, string> = { check_in: "وصول", check_out: "مغادرة", cleaning: "تنظيف", maintenance: "صيانة", guest_support: "دعم ضيف", handoff: "تسليم" };
const statusLabel: Record<OperationsTaskItem["status"], string> = { open: "مفتوحة", in_progress: "قيد التنفيذ", completed: "مكتملة", cancelled: "ملغاة" };

function formatDate(value: string | null) { return value ? new Intl.DateTimeFormat("ar-EG", { dateStyle: "medium", timeStyle: "short" }).format(new Date(value)) : "بدون موعد"; }

function Feedback({ state }: Readonly<{ state: TaskActionState }>) { return state.status === "idle" || !state.message ? null : <p aria-live="polite" className={`mt-3 text-xs font-semibold ${state.status === "success" ? "text-tide" : state.status === "denied" ? "text-coral" : "text-[#85652e]"}`}><CircleAlert aria-hidden="true" className="me-1 inline size-3.5" />{state.message}</p>; }

function CreateTaskForm({ createTask }: Readonly<{ createTask: CreateTaskAction }>) {
  const [state, action, pending] = useActionState(createTask, initialState);
  const { formRef, idempotencyKey } = useCommandForm(state);
  return <form action={action} className="rounded-[1.6rem] border border-[#d4dfda] bg-[#f0f7f4] p-5 shadow-[0_12px_30px_rgba(16,33,38,0.04)] sm:p-6" ref={formRef}><input name="idempotency_key" type="hidden" value={idempotencyKey} /><div className="flex items-start gap-3"><div className="grid size-10 place-items-center rounded-xl bg-harbor text-sea-glass"><Plus aria-hidden="true" className="size-5" /></div><div><h2 className="text-xl font-extrabold tracking-[-0.07em] text-harbor">مهمة تشغيل جديدة</h2><p className="mt-1 text-xs leading-6 text-muted">سجّل الخطوة اليدوية التالية، واربطها بحجز من خلال command الموثوق.</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-xs font-bold text-harbor" htmlFor="task-type">النوع<select className="mt-2 h-12 w-full rounded-xl border border-[#c9d9d3] bg-white px-3 text-sm font-normal text-ink outline-none focus:border-tide focus:ring-4 focus:ring-sea-glass/35" defaultValue="check_in" disabled={pending} id="task-type" name="task_type"><option value="check_in">وصول</option><option value="check_out">مغادرة</option><option value="cleaning">تنظيف</option><option value="maintenance">صيانة</option><option value="guest_support">دعم ضيف</option><option value="handoff">تسليم</option></select></label><label className="text-xs font-bold text-harbor" htmlFor="task-due">الموعد <span className="font-normal text-muted">(اختياري)</span><input className="mt-2 h-12 w-full rounded-xl border border-[#c9d9d3] bg-white px-3 text-sm text-ink outline-none focus:border-tide focus:ring-4 focus:ring-sea-glass/35" disabled={pending} id="task-due" name="due_at" type="datetime-local" /></label></div><label className="mt-4 block text-xs font-bold text-harbor" htmlFor="task-title">عنوان المهمة<input className="mt-2 h-12 w-full rounded-xl border border-[#c9d9d3] bg-white px-3 text-sm text-ink outline-none focus:border-tide focus:ring-4 focus:ring-sea-glass/35" disabled={pending} id="task-title" maxLength={200} name="title" placeholder="مثال: تجهيز مفاتيح الوصول" required /></label><label className="mt-4 block text-xs font-bold text-harbor" htmlFor="task-description">التفاصيل <span className="font-normal text-muted">(اختياري)</span><textarea className="mt-2 min-h-20 w-full rounded-xl border border-[#c9d9d3] bg-white p-3 text-sm font-normal text-ink outline-none focus:border-tide focus:ring-4 focus:ring-sea-glass/35" disabled={pending} id="task-description" maxLength={2000} name="description" /></label><div className="mt-5 flex items-center justify-between gap-3 border-t border-[#d4dfda] pt-4"><p className="text-[11px] text-muted">المهمة لا تغيّر حالة الحجز تلقائياً.</p><button className="flex min-h-11 items-center gap-2 rounded-xl bg-harbor px-5 text-xs font-bold text-white hover:bg-tide disabled:opacity-50" disabled={pending} type="submit">{pending ? <LoaderCircle aria-hidden="true" className="size-4 animate-spin" /> : <ClipboardList aria-hidden="true" className="size-4" />}إضافة المهمة</button></div><Feedback state={state} /></form>;
}

function TaskCard({ task, updateStatus }: Readonly<{ task: OperationsTaskItem; updateStatus: UpdateTaskStatusAction }>) {
  return <article className={`rounded-[1.35rem] border bg-surface p-5 shadow-[0_10px_24px_rgba(16,33,38,0.03)] ${task.status === "completed" ? "border-line opacity-75" : "border-[#d4dfda]"}`}><div className="flex items-start justify-between gap-4"><div className="min-w-0"><p className="text-[10px] font-bold text-tide">{taskTypeLabel[task.taskType] ?? task.taskType}</p><h3 className="mt-2 truncate text-base font-extrabold tracking-[-0.05em] text-harbor">{task.title}</h3></div><span className={`shrink-0 rounded-full px-2.5 py-1 text-[10px] font-bold ${task.status === "completed" ? "bg-sea-glass text-tide" : task.status === "cancelled" ? "bg-[#f1f0ed] text-muted" : "bg-[#fff8e9] text-[#85652e]"}`}>{statusLabel[task.status]}</span></div>{task.description ? <p className="mt-4 text-xs leading-6 text-muted">{task.description}</p> : null}<div className="mt-5 flex flex-wrap items-center justify-between gap-3 border-t border-line pt-3 text-[11px] text-muted"><span className="inline-flex items-center gap-1.5"><Clock3 aria-hidden="true" className="size-3.5 text-tide" />{formatDate(task.dueAt)}</span>{task.bookingId ? <bdi className="font-mono text-[10px] text-ink" dir="ltr">حجز {task.bookingId.slice(0, 8)}</bdi> : <span>غير مرتبطة بحجز</span>}</div>{task.status !== "completed" && task.status !== "cancelled" ? <div className="mt-4 flex flex-wrap gap-2"><form action={updateStatus.bind(null, task.id, "in_progress")}><button className="min-h-10 rounded-xl border border-[#bfd1cb] bg-white px-3 text-[11px] font-bold text-tide hover:bg-sea-glass/35" type="submit">قيد التنفيذ</button></form><form action={updateStatus.bind(null, task.id, "completed")}><button className="inline-flex min-h-10 items-center gap-1.5 rounded-xl bg-tide px-3 text-[11px] font-bold text-white hover:bg-harbor" type="submit"><CheckCircle2 aria-hidden="true" className="size-3.5" />إكمال</button></form></div> : null}</article>;
}

export function OperationsTasksPage({ tasks, createTask, updateStatus }: Readonly<{ tasks: readonly OperationsTaskItem[]; createTask: CreateTaskAction; updateStatus: UpdateTaskStatusAction }>) {
  const open = tasks.filter((task) => task.status === "open").length;
  const inProgress = tasks.filter((task) => task.status === "in_progress").length;
  const completed = tasks.filter((task) => task.status === "completed").length;
  return <main className="min-h-[calc(100vh-74px)] bg-canvas px-4 py-6 text-ink sm:px-7 sm:py-8 lg:px-9 lg:py-10"><div className="mx-auto max-w-[1180px]"><header className="rounded-[2rem] border border-[#d4dfda] bg-[#f0f7f4] px-6 py-7 shadow-[0_18px_44px_rgba(16,33,38,0.05)] sm:px-9 sm:py-9"><div className="flex flex-wrap items-start justify-between gap-5"><div className="flex gap-4"><div className="grid size-12 shrink-0 place-items-center rounded-2xl bg-harbor text-sea-glass"><ClipboardList aria-hidden="true" className="size-6" /></div><div><p className="text-[11px] font-bold tracking-[0.08em] text-tide">تنفيذ يومي</p><h1 className="mt-2 text-3xl font-bold tracking-[-0.09em] text-harbor sm:text-4xl">مهام التشغيل</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-muted">حوّل الخطوات المتكررة إلى قائمة واضحة للفريق، مع حالة وموعد ودليل تدقيق لكل تغيير.</p></div></div><div className="flex items-center gap-2 rounded-xl border border-[#d4dfda] bg-white/70 px-3 py-2 text-[11px] font-semibold text-tide"><Wrench aria-hidden="true" className="size-4" />يدوي ومراقَب</div></div><div className="mt-7 grid gap-3 border-t border-[#d4dfda] pt-5 sm:grid-cols-3"><div><p className="font-mono text-3xl font-medium text-harbor">{open}</p><p className="mt-1 text-xs text-muted">مفتوحة</p></div><div><p className="font-mono text-3xl font-medium text-[#85652e]">{inProgress}</p><p className="mt-1 text-xs text-muted">قيد التنفيذ</p></div><div><p className="font-mono text-3xl font-medium text-tide">{completed}</p><p className="mt-1 text-xs text-muted">مكتملة</p></div></div></header><div className="mt-8 grid gap-6 xl:grid-cols-[minmax(0,0.82fr)_minmax(0,1.18fr)]"><CreateTaskForm createTask={createTask} /><section aria-labelledby="tasks-queue-heading"><div className="flex items-end justify-between gap-3"><div><p className="text-[11px] font-bold text-tide">قائمة اليوم</p><h2 className="mt-2 text-2xl font-extrabold tracking-[-0.08em] text-harbor" id="tasks-queue-heading">المهام</h2></div><span className="rounded-full bg-sea-glass px-3 py-1.5 text-[11px] font-bold text-tide">{tasks.length} إجمالي</span></div>{tasks.length === 0 ? <div className="mt-5 rounded-[1.6rem] border border-dashed border-[#bfd1cb] bg-surface px-6 py-14 text-center"><ClipboardList aria-hidden="true" className="mx-auto size-7 text-tide" /><h3 className="mt-4 text-lg font-extrabold text-harbor">لا توجد مهام بعد</h3><p className="mx-auto mt-2 max-w-md text-sm leading-7 text-muted">أضف أول خطوة تشغيلية لتبدأ قائمة العمل الحقيقية.</p></div> : <div className="mt-5 space-y-4">{tasks.map((task) => <TaskCard key={task.id} task={task} updateStatus={updateStatus} />)}</div>}</section></div></div></main>;
}
